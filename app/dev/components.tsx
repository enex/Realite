import { ScrollView, Text } from "react-native";

export default function DevComponentsPage() {
  return (
    <ScrollView contentInsetAdjustmentBehavior="automatic">
      <Text>This is a component page.</Text>
    </ScrollView>
  );
}
